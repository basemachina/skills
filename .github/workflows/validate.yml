name: Validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  skills:
    name: Skill repository checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate skill metadata
        run: python3 scripts/validate-skills.py
